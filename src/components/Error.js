import React from "react";

const Error = (Error) => {
  console.log(Error);
  return (
    <div>
      <h1 className="font-bold text-4xl">Error! page not found!</h1>
    </div>
  );
};

export default Error;
